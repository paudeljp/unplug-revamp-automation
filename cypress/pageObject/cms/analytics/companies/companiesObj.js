import * as filterModule from "./layout/filterModule";

class companiesModule {
  init() {
    if (0 == 0) {
      context("Filter", () => {
        filterModule.filterObj();
      });
    }
  }
}

export default companiesModule;
