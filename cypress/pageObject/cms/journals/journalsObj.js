import * as editModule from "./layout/editModule";

class journalsModule {
  init() {
    if (0 == 0) {
      context("Edit", () => {
        editModule.editObj();
      });
    }
  }
}

export default journalsModule;
